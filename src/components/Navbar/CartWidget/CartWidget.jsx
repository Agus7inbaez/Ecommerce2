import cart from '../CartWidget/assets/Carrito.svg'

const CartWidget = () => {
    return (
        <button>
            <img src={'cart'} />
            0
        </button>
    )
}

export default CartWidget